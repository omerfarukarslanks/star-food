import { PhoneMaskDirective } from './phone-mask.directive';

describe('PhoneMaskDirective', () => {
  it('should create an instance', () => {
    const directive = PhoneMaskDirective
    expect(directive).toBeTruthy();
  });
});

describe('Directive input control', () => {

})
